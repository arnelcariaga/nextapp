import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "COIN | Formulario inscripción de participantes",
  description: "",
};

export default function ParticipantInscriptionFormLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children
}
