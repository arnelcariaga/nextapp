import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "COIN | Operativo Comunidad -> Agregar",
  description: "",
};

export default function CommunityOperationsAddLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children
}
