import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "COIN | Operativo Comunidad -> Lista de pacientes",
  description: "",
};

export default function CommunityOperationsUserListLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children
}
