import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "COIN | Operativo Comunidad -> Pacientes",
  description: "",
};

export default function CommunityOperationUsersLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children
}
