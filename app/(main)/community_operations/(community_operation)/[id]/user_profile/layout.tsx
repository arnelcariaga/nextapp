import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "COIN | Operativo Comunidad -> Paciente -> Perfil",
  description: "",
};

export default function CommunityOperationUserProfileLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children
}
