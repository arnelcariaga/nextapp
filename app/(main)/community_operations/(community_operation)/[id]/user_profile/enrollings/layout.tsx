import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "COIN | Operativo Comunidad -> Paciente -> Perfil -> Situación enrolamiento",
  description: "",
};

export default function CommunityOperationUserProfileEnrollingsLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children
}
