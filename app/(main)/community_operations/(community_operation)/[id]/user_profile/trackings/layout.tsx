import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "COIN | Operativo Comunidad -> Paciente -> Perfil -> Seguimientos",
  description: "",
};

export default function CommunityOperationUserProfileTrackingsLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children
}
