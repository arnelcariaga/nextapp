import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "COIN | Operativo Comunidad",
  description: "",
};

export default function CommunityOperationsLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children
}
